# CLI 表格编辑器 - 设计文档

## 项目概述

一个在命令行终端显示和编辑表格的工具，支持新建、编辑、查看表格，数据保存在 Python 文件中，可导出到 Excel。

## 使用方式

```bash
# 编辑现有表格文件
excel-cli path/to/data.py

# 新建表格文件
excel-cli new_table.py
```

程序启动后会读取指定 Python 文件中的 `data` 变量，编辑后保存回该文件。

## 需求确认

### 1. 界面交互方式
**选择：交互式 TUI（终端用户界面）**
- 全屏终端界面，类似 htop 或 midnight commander
- 提供面板、菜单、实时单元格编辑

### 2. 数据存储格式
**选择：Python 字典字面量**
- 简单的 `data = {...}` 字典格式
- 易于阅读和手动修改

### 3. Excel 导出功能
**选择：基础导出**
- 导出为 .xlsx 文件
- 仅包含基础数据，无复杂格式或公式

### 4. 多表格支持
**选择：单表格模式**
- 简化界面
- 一个 Python 文件存储一个表格

### 5. 公式支持
**选择：基础公式支持**
- 支持 SUM、AVG、COUNT、MIN、MAX 等常用函数
- 支持单元格引用（如 A1、B2:C5）
- 公式以 `=` 开头标识

### 6. 自定义数据接口
**选择：支持自定义接口获取数据**
- 可定义 Python 函数作为数据源
- 单元格可引用自定义函数返回值
- 支持动态数据刷新

## 技术方案对比

### 方案 A：Textual 框架（推荐）
- **优点**：现代 Python TUI 框架，样式丰富，响应式，异步支持
- **缺点**：额外依赖（约 2MB）
- **适用**：追求最佳用户体验和开发效率

### 方案 B：curses 标准库
- **优点**：Python 内置，无额外依赖
- **缺点**：底层 API，需要更多手动编码
- **适用**：对依赖有严格限制的场景

### 方案 C：Rich 库
- **优点**：简单易用，流行度高
- **缺点**：交互能力有限，更像显示库而非应用框架
- **适用**：简单显示场景

## 推荐方案

**采用 Textual 框架**，理由：
1. 最佳的 TUI 用户体验
2. 代码量最少
3. 组件模型清晰，易于维护

## 核心功能模块

```
excel-cli/
├── excel_cli/
│   ├── __init__.py
│   ├── cli.py           # CLI 入口点，解析命令行参数
│   ├── app.py           # Textual 应用主类
│   ├── widgets/
│   │   ├── __init__.py
│   │   ├── table.py     # 表格编辑组件
│   │   ├── header.py    # 标题栏组件
│   │   └── footer.py    # 状态栏/快捷键提示
│   ├── storage.py       # 数据读写模块（加载/保存 .py 文件）
│   ├── formula.py       # 公式解析和计算引擎
│   ├── custom.py        # 自定义数据接口模块
│   └── export.py        # Excel 导出模块
├── pyproject.toml       # 项目配置，包含 CLI 入口点
└── README.md
```

## CLI 入口点配置

```toml
# pyproject.toml
[project.scripts]
excel-cli = "excel_cli.cli:main"
```

## 数据存储格式示例

用户使用 `excel-cli my_table.py` 打开的文件格式：

```python
# my_table.py - 由 excel-cli 自动生成和编辑

# 表格数据
data = {
    "headers": ["姓名", "年龄", "城市", "得分"],
    "rows": [
        ["张三", 28, "北京", 85],   # 第1行 (A1-D1)
        ["李四", 32, "上海", 92],   # 第2行 (A2-D2)
        ["王五", 25, "广州", 78],   # 第3行 (A3-D3)
        ["合计", "=SUM(B1:B3)", "", "=SUM(D1:D3)"]  # 第4行 (A4-D4)
    ]
}

# 公式函数定义（可选）
# B1=28, B2=32, B3=25 -> SUM=85
# D1=85, D2=92, D3=78 -> SUM=255

# 公式函数定义（可选）
formulas = {
    "SUM": lambda cells: sum(cells),
    "AVG": lambda cells: sum(cells) / len(cells) if cells else 0,
    "COUNT": lambda cells: len(cells),
    "MIN": lambda cells: min(cells) if cells else 0,
    "MAX": lambda cells: max(cells) if cells else 0,
}

# 自定义数据接口（可选）
def get_api_data(endpoint: str) -> list:
    """从自定义接口获取数据"""
    import urllib.request
    import json
    with urllib.request.urlopen(endpoint) as response:
        return json.loads(response.read())

custom_functions = {
    "fetch_users": lambda: get_api_data("https://api.example.com/users"),
    "current_time": lambda: __import__("datetime").datetime.now().strftime("%Y-%m-%d"),
}
```

## 公式语法

| 公式 | 说明 |
|------|------|
| `=SUM(A1:A5)` | 求和 |
| `=AVG(B1:B10)` | 平均值 |
| `=COUNT(C1:C20)` | 计数 |
| `=MIN(D1:D5)` | 最小值 |
| `=MAX(E1:E5)` | 最大值 |
| `=CUSTOM.fetch_users()` | 调用自定义函数 |

## 交互设计

| 快捷键 | 功能 |
|--------|------|
| ↑↓←→ | 移动单元格 |
| Enter | 编辑单元格 |
| Escape | 退出编辑/返回 |
| Ctrl+S | 保存 |
| Ctrl+E | 导出 Excel |
| Ctrl+N | 新建表格 |
| Ctrl+R | 刷新公式/自定义数据 |
| Ctrl+F | 插入公式 |
| q | 退出程序 |

## 下一步

1. ~~确认设计方案~~（已完成）
2. ~~添加公式和自定义接口支持~~（已完成）
3. ~~添加 CLI 使用方式~~（已完成）
4. 创建详细实现计划
5. 开始编码实现